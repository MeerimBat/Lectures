import random

print(random.randrange(1, 100))
number_to_guess =(random.randrange(1, 100) )
attempts = 0
print ("A number has been generated. You have 3 attempts to guess it")
guess=random.randrange
if guess>number_to_guess:
    print("Too high!")
#Project: Number Guessing Game. The program chooses a secret random number between 1 and 100. The user has a limited number of attempts to guess it.
#fter each guess, the program tells the user if their guess was "too high," "too low," or "correct."

